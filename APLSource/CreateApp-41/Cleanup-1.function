 r←Cleanup name;rc;flag;counter
⍝ Delete the folders "name" recursively.
 r←⍬
 :If #.FilesAndDirs.Exists name
     flag←0
     counter←0
     :Repeat
         rc←#.FilesAndDirs.RmDir name
         :If 0=0⊃rc
             flag←1
         :Else
             ⎕DL 1
             ⎕←'Waiting...'
         :EndIf
     :Until flag∨10>counter←counter+1
 :EndIf
